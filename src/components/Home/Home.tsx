// Redux
// import { useAppSelector } from "../../hooks";

const Home = () => {
  // const { user } = useAppSelector((state) => state.auth);
  // const { hierarchy } = user;

  // if (hierarchy === "admin") return <div>Admin</div>;
  // if (hierarchy === "user") return <div>User</div>;
  // if (hierarchy === "guest") return <div>Guest</div>;
  // else return <div>No rol</div>;

  return <div>Soy un Home</div>;
};

export { Home };
